本地开发需先安装 Ruby/RubyGems/GCC/Make，然后执行 `bash run_server.sh`（等价于 `bundle exec jekyll serve --livereload`）启动热重载服务；Google Scholar 引用统计通过仓库 Secrets 中的 `GOOGLE_SCHOLAR_ID` 配合 GitHub Actions 自动生成到 `google-scholar-stats` 分支。