- 站点全局常量统一集中在 `_config.yml` 中按区块（Site Settings / Author / SEO / Plugins 等）组织，而非分散在各页面 YAML front matter。
- Ruby/Jekyll 依赖通过 `Gemfile` 显式声明并随仓库提交 `Gemfile.lock`，确保本地与 CI 构建环境一致。
- Vercel 部署采用单一 `vercel.json` 重写规则将全部 URL 指向 `index.html`，不区分具体页面路由。